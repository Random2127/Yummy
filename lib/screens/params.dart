import 'package:flutter/material.dart';

class ParamsScreen extends StatelessWidget {
  const ParamsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Parameters screen'));
  }
}
